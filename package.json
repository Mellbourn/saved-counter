{
  "name": "saved-counter",
  "version": "1.0.0",
  "description": "AWS Serverless Application Model based graphql app for storing counters online",
  "repository": "https://github.com/Mellbourn/saved-counter",
  "author": "Klas Mellbourn <klas@mellbourn.net>",
  "license": "MIT",
  "scripts": {
    "build": "pushd counter && yarn build && popd && sam build",
    "start": "yarn build && sam deploy --guided",
    "start:local": "yarn build && sam local invoke",
    "clean": "aws cloudformation delete-stack --stack-name saved-counter-stack"
  }
}
