{
  "name": "saved-counter",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/client",
      "packages/server"
    ]
  },
  "scripts": {
    "start": "yarn workspaces foreach -p run start"
  }
}
